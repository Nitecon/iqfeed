package iqfeed

import "testing"

func TestStart(t *testing.T) {
	/*dataChan := make(chan []byte)
	c, err := net.Dial("tcp", "127.0.0.1:5009")
	if err != nil {
		log.Fatal("Could not connect to IQFeed")
	}

	c.Close()
	*/

}
